package datanormalizationtool;

/**
 * Enum of cell flags. *This might makes sense to be called error.
 */
public enum Error {
  AGE_GRADE_MISMATCH,
  INVALID_GRADE,
  NO_ERROR,
  NO_MIDDLE_NAME,
  UNKNOWN_TOWN,
}
